# Override this file with .env.local for changes for your environment
# Supported values:
# VITE_AUTH = [enabled|disabled|proxy]
# VITE_BASE_PATH = <path>
#
# These values only configure the Vite development server. Production builds
# receive authentication mode and base path from `phenix ui` at runtime.

VITE_AUTH=enabled